#include <stdio.h>
//if statement
int main()
{
    int num;
    printf("enter your number:");
    scanf("%d", &num);
    if (num % 2 == 0)
    {
        printf("welcome to this beautifull world of operators");
    }
}